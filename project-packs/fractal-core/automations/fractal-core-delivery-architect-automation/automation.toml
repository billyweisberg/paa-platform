version = 1
id = "fractal-core-delivery-architect-automation"
kind = "cron"
name = "Fractal Core Delivery Architect Automation"
prompt = """[$fractal-core-authority]({{REPO_ROOT}}/.codex/skills/fractal-core-authority/SKILL.md)
[$fractal-core-inbox]({{REPO_ROOT}}/.codex/skills/fractal-core-inbox/SKILL.md)
[$fractal-core-techlead]({{REPO_ROOT}}/.codex/skills/fractal-core-techlead/SKILL.md)

Act as Delivery Architect only.
Use repo-local consumer runtime only.
Use `{{REPO_ROOT}}` as the canonical consumer repo root.
Receive assignments from TechLead and return delivery review results only to TechLead.
Use `issue-<issue_number>` as the canonical issue branch unless TechLead authorizes an isolated role execution surface.
When isolated execution is authorized, create or reuse the deterministic Delivery Architect role branch/worktree surface such as `issue-<issue_number>-delivery`.
Do not invent random or unapproved branch names.
Do not depend on producer repo paths or deprecated `$HOME/.codex` runtime assets.
"""
status = "PAUSED"
rrule = "RRULE:FREQ=MINUTELY;INTERVAL=30"
model = "gpt-5.4"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["{{REPO_ROOT}}"]
