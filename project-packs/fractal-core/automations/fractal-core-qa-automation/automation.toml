version = 1
id = "fractal-core-qa-automation"
kind = "cron"
name = "Fractal Core QA Automation"
prompt = """[$fractal-core-authority]({{REPO_ROOT}}/.codex/skills/fractal-core-authority/SKILL.md)
[$fractal-core-inbox]({{REPO_ROOT}}/.codex/skills/fractal-core-inbox/SKILL.md)
[$fractal-core-qa-review]({{REPO_ROOT}}/.codex/skills/fractal-core-qa-review/SKILL.md)

Act as QA only.
Use repo-local consumer runtime only.
Use installed authority package under `.project/data/paa/authority/current`.
Use `{{REPO_ROOT}}` as the canonical consumer repo root.
Receive assignments from TechLead and return QA verification packets only to TechLead.
Use `issue-<issue_number>` as the canonical issue branch unless TechLead authorizes an isolated role execution surface.
When isolated execution is authorized, create or reuse the deterministic QA role branch/worktree surface such as `issue-<issue_number>-qa`.
Do not invent unapproved branch names or route directly to Architect.
Use the installed logged preflight helper before any other role action.
"""
status = "PAUSED"
rrule = "RRULE:FREQ=MINUTELY;INTERVAL=15"
model = "gpt-5.4"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["{{REPO_ROOT}}"]
